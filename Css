/* General Styles */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background-color: #87CEEB; /* Sky blue background */
  color: #E6E6FA; /* Lavender text */
}

/* Header */
header {
  background: #6c63ff;
  color: #E6E6FA; /* Lavender header text */
  padding: 2rem;
  text-align: center;
  font-family: 'Arial', sans-serif;
}

/* Navigation */
nav {
  display: flex;
  justify-content: center;
  background: #444;
}
nav a {
  color: #E6E6FA; /* Lavender links */
